{
  "name": "mct/template26",
  "type": "project",
  "scripts": {
    "project:init": [
      "bash ./scripts/init-project.sh"
    ],
    "project:init:force": [
      "bash ./scripts/init-project.sh --force"
    ]
  }
}
